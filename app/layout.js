export const metadata = {
  title: 'とんがり親分 iNFT',
  description: '語りかけてくるとんがり親分',
}

export default function RootLayout({ children }) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  )
}
